<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.spring.board.BoardMapper">

    <select id="boardList" resultType="BoardDomain">
        SELECT A.iboard, A.title, A.regdt, B.unm as writerNm, B.profileImg
        FROM t_board A
        INNER  JOIN t_user B
        ON A.iuser = B.iuser
        ORDER BY A.iboard DESC
    </select>

    <select id="boardDetail" resultType="BoardDomain">
        SELECT A.iboard, A.title, A.regdt, A.ctnt, B.unm as writerNm, B.profileImg
        FROM t_board A
        INNER  JOIN t_user B
        ON A.iuser = B.iuser
        WHERE A.iboard = #{iboard}
    </select>
</mapper>
